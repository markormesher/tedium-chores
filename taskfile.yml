# This file is maintained by Tedium - manual edits will be overwritten!

version: "3"
includes:
  local:
    taskfile: taskfile.local.yml
    optional: true
tasks:
  cachekey:
    cmds:
      - task: cachekey-automergeprs-go
      - task: cachekey-generatetasksandci-go
      - task: cachekey-updateimagelabels-go
  cachekey-automergeprs:
    cmds:
      - task: cachekey-automergeprs-go
  cachekey-automergeprs-go:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    generates:
      - .task-meta-cache-key
    cmds:
      - cmd: |-
          if [[ -n ${CI:-} ]]; then
            if [[ -n ${FORGEJO_REPOSITORY:-} ]]; then
              PROJECT=$(echo "$FORGEJO_REPOSITORY" | tr -dc '[[a-z0-9]]')
            elif [[ -n ${GITHUB_REPOSITORY:-} ]]; then
              PROJECT=$(echo "$GITHUB_REPOSITORY" | tr -dc '[[a-z0-9]]')
            else
              PROJECT="noproject"
            fi

            DEPS_SHA=$(cat go.mod | sha256sum | awk '{ print $1 }')

            if [[ -f go.sum ]]; then
              LOCK_SHA=$(cat go.sum | sha256sum | awk '{ print $1 }')
            else
              LOCK_SHA="nolock"
            fi

            echo "${PROJECT}-go-v1/${DEPS_SHA}/${LOCK_SHA}" > .task-meta-cache-key
          fi
  cachekey-generatetasksandci:
    cmds:
      - task: cachekey-generatetasksandci-go
  cachekey-generatetasksandci-go:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    generates:
      - .task-meta-cache-key
    cmds:
      - cmd: |-
          if [[ -n ${CI:-} ]]; then
            if [[ -n ${FORGEJO_REPOSITORY:-} ]]; then
              PROJECT=$(echo "$FORGEJO_REPOSITORY" | tr -dc '[[a-z0-9]]')
            elif [[ -n ${GITHUB_REPOSITORY:-} ]]; then
              PROJECT=$(echo "$GITHUB_REPOSITORY" | tr -dc '[[a-z0-9]]')
            else
              PROJECT="noproject"
            fi

            DEPS_SHA=$(cat go.mod | sha256sum | awk '{ print $1 }')

            if [[ -f go.sum ]]; then
              LOCK_SHA=$(cat go.sum | sha256sum | awk '{ print $1 }')
            else
              LOCK_SHA="nolock"
            fi

            echo "${PROJECT}-go-v1/${DEPS_SHA}/${LOCK_SHA}" > .task-meta-cache-key
          fi
  cachekey-updateimagelabels:
    cmds:
      - task: cachekey-updateimagelabels-go
  cachekey-updateimagelabels-go:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    generates:
      - .task-meta-cache-key
    cmds:
      - cmd: |-
          if [[ -n ${CI:-} ]]; then
            if [[ -n ${FORGEJO_REPOSITORY:-} ]]; then
              PROJECT=$(echo "$FORGEJO_REPOSITORY" | tr -dc '[[a-z0-9]]')
            elif [[ -n ${GITHUB_REPOSITORY:-} ]]; then
              PROJECT=$(echo "$GITHUB_REPOSITORY" | tr -dc '[[a-z0-9]]')
            else
              PROJECT="noproject"
            fi

            DEPS_SHA=$(cat go.mod | sha256sum | awk '{ print $1 }')

            if [[ -f go.sum ]]; then
              LOCK_SHA=$(cat go.sum | sha256sum | awk '{ print $1 }')
            else
              LOCK_SHA="nolock"
            fi

            echo "${PROJECT}-go-v1/${DEPS_SHA}/${LOCK_SHA}" > .task-meta-cache-key
          fi
  cacheload:
    cmds:
      - task: cacheload-automergeprs-go
      - task: cacheload-generatetasksandci-go
      - task: cacheload-updateimagelabels-go
  cacheload-automergeprs:
    cmds:
      - task: cacheload-automergeprs-go
  cacheload-automergeprs-go:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    deps:
      - cachekey-automergeprs-go
    cmds:
      - cmd: |-
          if [[ -n "${CI_CACHE_TOKEN:-}" ]] && [[ -f ".task-meta-cache-key" ]]; then
            request_key=$(cat ".task-meta-cache-key" | tr -d '\r\n')
            start_ts=$(date +%s)
            header_file=$(mktemp)
            cache_file=$(mktemp)

            echo "loading cache..."
            echo "request key: ${request_key}"
            if curl -fsSL -D "${header_file}" -H "authorization: Bearer ${CI_CACHE_TOKEN}" "https://ci-cache.markormesher.co.uk/cache/${request_key}" -o "${cache_file}"; then
              actual_key=$(cat "${header_file}" | grep -i x-cache-key | cut -d ' ' -f 2 | tr -d '\r\n')
              size=$(du -h "${cache_file}" | awk '{ print $1 }')
              echo "received key: ${actual_key}"
              echo "received size: ${size}"

              if [[ "${actual_key}" == "${request_key}" ]]; then
                touch .task-meta-cache-exact-match
              fi

              echo "unpacking cache..."
              tar xzP -f "${cache_file}" || true
            else
              echo "no cache loaded"
            fi

            rm -f "${cache_file}" "${header_file}"
            end_ts=$(date +%s)
            echo "loading cache took $(( $end_ts - $start_ts ))s"
          fi
  cacheload-generatetasksandci:
    cmds:
      - task: cacheload-generatetasksandci-go
  cacheload-generatetasksandci-go:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    deps:
      - cachekey-generatetasksandci-go
    cmds:
      - cmd: |-
          if [[ -n "${CI_CACHE_TOKEN:-}" ]] && [[ -f ".task-meta-cache-key" ]]; then
            request_key=$(cat ".task-meta-cache-key" | tr -d '\r\n')
            start_ts=$(date +%s)
            header_file=$(mktemp)
            cache_file=$(mktemp)

            echo "loading cache..."
            echo "request key: ${request_key}"
            if curl -fsSL -D "${header_file}" -H "authorization: Bearer ${CI_CACHE_TOKEN}" "https://ci-cache.markormesher.co.uk/cache/${request_key}" -o "${cache_file}"; then
              actual_key=$(cat "${header_file}" | grep -i x-cache-key | cut -d ' ' -f 2 | tr -d '\r\n')
              size=$(du -h "${cache_file}" | awk '{ print $1 }')
              echo "received key: ${actual_key}"
              echo "received size: ${size}"

              if [[ "${actual_key}" == "${request_key}" ]]; then
                touch .task-meta-cache-exact-match
              fi

              echo "unpacking cache..."
              tar xzP -f "${cache_file}" || true
            else
              echo "no cache loaded"
            fi

            rm -f "${cache_file}" "${header_file}"
            end_ts=$(date +%s)
            echo "loading cache took $(( $end_ts - $start_ts ))s"
          fi
  cacheload-updateimagelabels:
    cmds:
      - task: cacheload-updateimagelabels-go
  cacheload-updateimagelabels-go:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    deps:
      - cachekey-updateimagelabels-go
    cmds:
      - cmd: |-
          if [[ -n "${CI_CACHE_TOKEN:-}" ]] && [[ -f ".task-meta-cache-key" ]]; then
            request_key=$(cat ".task-meta-cache-key" | tr -d '\r\n')
            start_ts=$(date +%s)
            header_file=$(mktemp)
            cache_file=$(mktemp)

            echo "loading cache..."
            echo "request key: ${request_key}"
            if curl -fsSL -D "${header_file}" -H "authorization: Bearer ${CI_CACHE_TOKEN}" "https://ci-cache.markormesher.co.uk/cache/${request_key}" -o "${cache_file}"; then
              actual_key=$(cat "${header_file}" | grep -i x-cache-key | cut -d ' ' -f 2 | tr -d '\r\n')
              size=$(du -h "${cache_file}" | awk '{ print $1 }')
              echo "received key: ${actual_key}"
              echo "received size: ${size}"

              if [[ "${actual_key}" == "${request_key}" ]]; then
                touch .task-meta-cache-exact-match
              fi

              echo "unpacking cache..."
              tar xzP -f "${cache_file}" || true
            else
              echo "no cache loaded"
            fi

            rm -f "${cache_file}" "${header_file}"
            end_ts=$(date +%s)
            echo "loading cache took $(( $end_ts - $start_ts ))s"
          fi
  cachesave:
    cmds:
      - task: cachesave-automergeprs-go
      - task: cachesave-generatetasksandci-go
      - task: cachesave-updateimagelabels-go
  cachesave-automergeprs:
    cmds:
      - task: cachesave-automergeprs-go
  cachesave-automergeprs-go:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    deps:
      - cachekey-automergeprs-go
    cmds:
      - cmd: echo "$(go env GOMODCACHE) $(go env GOCACHE)" > .task-meta-cache-paths
      - cmd: |-
          if [[ -n "${CI_CACHE_TOKEN:-}" ]] && [[ -f ".task-meta-cache-key" ]]; then
            if [[ -f .task-meta-cache-exact-match ]]; then
              echo "skipping re-upload because an exact match was returned from the cache"
              exit 0
            fi

            request_key=$(cat ".task-meta-cache-key")
            start_ts=$(date +%s)
            cache_file=$(mktemp)

            echo "packing cache..."
            tar czP -f "${cache_file}" $(cat .task-meta-cache-paths)
            size=$(du -h "${cache_file}" | awk '{ print $1 }')

            echo "uploading cache (${size})..."
            if curl -fsSL -H "authorization: Bearer ${CI_CACHE_TOKEN}" -X PUT "https://ci-cache.markormesher.co.uk/cache/${request_key}" --data-binary "@${cache_file}"; then
              echo "uploaded cache"
            else
              echo "error saving cache"
            fi

            rm -f "${cache_file}"
            end_ts=$(date +%s)
            echo "saving cache took $(( $end_ts - $start_ts ))s"
          fi
  cachesave-generatetasksandci:
    cmds:
      - task: cachesave-generatetasksandci-go
  cachesave-generatetasksandci-go:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    deps:
      - cachekey-generatetasksandci-go
    cmds:
      - cmd: echo "$(go env GOMODCACHE) $(go env GOCACHE)" > .task-meta-cache-paths
      - cmd: |-
          if [[ -n "${CI_CACHE_TOKEN:-}" ]] && [[ -f ".task-meta-cache-key" ]]; then
            if [[ -f .task-meta-cache-exact-match ]]; then
              echo "skipping re-upload because an exact match was returned from the cache"
              exit 0
            fi

            request_key=$(cat ".task-meta-cache-key")
            start_ts=$(date +%s)
            cache_file=$(mktemp)

            echo "packing cache..."
            tar czP -f "${cache_file}" $(cat .task-meta-cache-paths)
            size=$(du -h "${cache_file}" | awk '{ print $1 }')

            echo "uploading cache (${size})..."
            if curl -fsSL -H "authorization: Bearer ${CI_CACHE_TOKEN}" -X PUT "https://ci-cache.markormesher.co.uk/cache/${request_key}" --data-binary "@${cache_file}"; then
              echo "uploaded cache"
            else
              echo "error saving cache"
            fi

            rm -f "${cache_file}"
            end_ts=$(date +%s)
            echo "saving cache took $(( $end_ts - $start_ts ))s"
          fi
  cachesave-updateimagelabels:
    cmds:
      - task: cachesave-updateimagelabels-go
  cachesave-updateimagelabels-go:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    deps:
      - cachekey-updateimagelabels-go
    cmds:
      - cmd: echo "$(go env GOMODCACHE) $(go env GOCACHE)" > .task-meta-cache-paths
      - cmd: |-
          if [[ -n "${CI_CACHE_TOKEN:-}" ]] && [[ -f ".task-meta-cache-key" ]]; then
            if [[ -f .task-meta-cache-exact-match ]]; then
              echo "skipping re-upload because an exact match was returned from the cache"
              exit 0
            fi

            request_key=$(cat ".task-meta-cache-key")
            start_ts=$(date +%s)
            cache_file=$(mktemp)

            echo "packing cache..."
            tar czP -f "${cache_file}" $(cat .task-meta-cache-paths)
            size=$(du -h "${cache_file}" | awk '{ print $1 }')

            echo "uploading cache (${size})..."
            if curl -fsSL -H "authorization: Bearer ${CI_CACHE_TOKEN}" -X PUT "https://ci-cache.markormesher.co.uk/cache/${request_key}" --data-binary "@${cache_file}"; then
              echo "uploaded cache"
            else
              echo "error saving cache"
            fi

            rm -f "${cache_file}"
            end_ts=$(date +%s)
            echo "saving cache took $(( $end_ts - $start_ts ))s"
          fi
  deps:
    cmds:
      - task: deps-automergeprs-go
      - task: deps-generatetasksandci-go
      - task: deps-updateimagelabels-go
  deps-automergeprs:
    cmds:
      - task: deps-automergeprs-go
  deps-automergeprs-go:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    cmds:
      - cmd: go mod download
      - cmd: (go tool || true) | (grep '\.' || true) | while read t; do go build -o /dev/null $t; done
  deps-generatetasksandci:
    cmds:
      - task: deps-generatetasksandci-go
  deps-generatetasksandci-go:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    cmds:
      - cmd: go mod download
      - cmd: (go tool || true) | (grep '\.' || true) | while read t; do go build -o /dev/null $t; done
  deps-updateimagelabels:
    cmds:
      - task: deps-updateimagelabels-go
  deps-updateimagelabels-go:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    cmds:
      - cmd: go mod download
      - cmd: (go tool || true) | (grep '\.' || true) | while read t; do go build -o /dev/null $t; done
  imgbuild:
    cmds:
      - task: imgbuild-applybranchprotection
      - task: imgbuild-automergeprs
      - task: imgbuild-generatetasksandci
      - task: imgbuild-managegotools
      - task: imgbuild-setgotoolchain
      - task: imgbuild-updateimagelabels
  imgbuild-applybranchprotection:
    dir: '{{.ROOT_DIR}}/apply-branch-protection'
    deps:
      - imgrefs-applybranchprotection
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          bud_opts=(
            --layers
            -f "Containerfile"
          )

          if [[ -f argfile.conf ]]; then
            bud_opts+=("--build-arg-file" "argfile.conf")
          fi

          # first build to get visible logs
          buildah "${buildah_opts[@]}" bud "${bud_opts[@]}"

          # Second (cached) build to get the image ID
          img=$(buildah "${buildah_opts[@]}" bud "${bud_opts[@]}" -q)

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | while read tag; do
              buildah "${buildah_opts[@]}" tag "$img" "${tag}"
              echo "Tagged ${tag}"
            done
          fi
  imgbuild-automergeprs:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    deps:
      - imgrefs-automergeprs
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          bud_opts=(
            --layers
            -f "Containerfile"
          )

          if [[ -f argfile.conf ]]; then
            bud_opts+=("--build-arg-file" "argfile.conf")
          fi

          # first build to get visible logs
          buildah "${buildah_opts[@]}" bud "${bud_opts[@]}"

          # Second (cached) build to get the image ID
          img=$(buildah "${buildah_opts[@]}" bud "${bud_opts[@]}" -q)

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | while read tag; do
              buildah "${buildah_opts[@]}" tag "$img" "${tag}"
              echo "Tagged ${tag}"
            done
          fi
  imgbuild-generatetasksandci:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    deps:
      - imgrefs-generatetasksandci
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          bud_opts=(
            --layers
            -f "Containerfile"
          )

          if [[ -f argfile.conf ]]; then
            bud_opts+=("--build-arg-file" "argfile.conf")
          fi

          # first build to get visible logs
          buildah "${buildah_opts[@]}" bud "${bud_opts[@]}"

          # Second (cached) build to get the image ID
          img=$(buildah "${buildah_opts[@]}" bud "${bud_opts[@]}" -q)

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | while read tag; do
              buildah "${buildah_opts[@]}" tag "$img" "${tag}"
              echo "Tagged ${tag}"
            done
          fi
  imgbuild-managegotools:
    dir: '{{.ROOT_DIR}}/manage-go-tools'
    deps:
      - imgrefs-managegotools
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          bud_opts=(
            --layers
            -f "Containerfile"
          )

          if [[ -f argfile.conf ]]; then
            bud_opts+=("--build-arg-file" "argfile.conf")
          fi

          # first build to get visible logs
          buildah "${buildah_opts[@]}" bud "${bud_opts[@]}"

          # Second (cached) build to get the image ID
          img=$(buildah "${buildah_opts[@]}" bud "${bud_opts[@]}" -q)

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | while read tag; do
              buildah "${buildah_opts[@]}" tag "$img" "${tag}"
              echo "Tagged ${tag}"
            done
          fi
  imgbuild-setgotoolchain:
    dir: '{{.ROOT_DIR}}/set-go-toolchain'
    deps:
      - imgrefs-setgotoolchain
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          bud_opts=(
            --layers
            -f "Containerfile"
          )

          if [[ -f argfile.conf ]]; then
            bud_opts+=("--build-arg-file" "argfile.conf")
          fi

          # first build to get visible logs
          buildah "${buildah_opts[@]}" bud "${bud_opts[@]}"

          # Second (cached) build to get the image ID
          img=$(buildah "${buildah_opts[@]}" bud "${bud_opts[@]}" -q)

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | while read tag; do
              buildah "${buildah_opts[@]}" tag "$img" "${tag}"
              echo "Tagged ${tag}"
            done
          fi
  imgbuild-updateimagelabels:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    deps:
      - imgrefs-updateimagelabels
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          bud_opts=(
            --layers
            -f "Containerfile"
          )

          if [[ -f argfile.conf ]]; then
            bud_opts+=("--build-arg-file" "argfile.conf")
          fi

          # first build to get visible logs
          buildah "${buildah_opts[@]}" bud "${bud_opts[@]}"

          # Second (cached) build to get the image ID
          img=$(buildah "${buildah_opts[@]}" bud "${bud_opts[@]}" -q)

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | while read tag; do
              buildah "${buildah_opts[@]}" tag "$img" "${tag}"
              echo "Tagged ${tag}"
            done
          fi
  imgpush:
    cmds:
      - task: imgpush-applybranchprotection
      - task: imgpush-automergeprs
      - task: imgpush-generatetasksandci
      - task: imgpush-managegotools
      - task: imgpush-setgotoolchain
      - task: imgpush-updateimagelabels
  imgpush-applybranchprotection:
    dir: '{{.ROOT_DIR}}/apply-branch-protection'
    deps:
      - imgrefs-applybranchprotection
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | (grep -v "^localhost" || :) | while read tag; do
              buildah "${buildah_opts[@]}" push "${tag}"
              echo "Pushed ${tag}"
            done
          else
            echo "No .task-meta-imgrefs file - nothing will be pushed"
            exit 1
          fi
  imgpush-automergeprs:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    deps:
      - imgrefs-automergeprs
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | (grep -v "^localhost" || :) | while read tag; do
              buildah "${buildah_opts[@]}" push "${tag}"
              echo "Pushed ${tag}"
            done
          else
            echo "No .task-meta-imgrefs file - nothing will be pushed"
            exit 1
          fi
  imgpush-generatetasksandci:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    deps:
      - imgrefs-generatetasksandci
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | (grep -v "^localhost" || :) | while read tag; do
              buildah "${buildah_opts[@]}" push "${tag}"
              echo "Pushed ${tag}"
            done
          else
            echo "No .task-meta-imgrefs file - nothing will be pushed"
            exit 1
          fi
  imgpush-managegotools:
    dir: '{{.ROOT_DIR}}/manage-go-tools'
    deps:
      - imgrefs-managegotools
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | (grep -v "^localhost" || :) | while read tag; do
              buildah "${buildah_opts[@]}" push "${tag}"
              echo "Pushed ${tag}"
            done
          else
            echo "No .task-meta-imgrefs file - nothing will be pushed"
            exit 1
          fi
  imgpush-setgotoolchain:
    dir: '{{.ROOT_DIR}}/set-go-toolchain'
    deps:
      - imgrefs-setgotoolchain
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | (grep -v "^localhost" || :) | while read tag; do
              buildah "${buildah_opts[@]}" push "${tag}"
              echo "Pushed ${tag}"
            done
          else
            echo "No .task-meta-imgrefs file - nothing will be pushed"
            exit 1
          fi
  imgpush-updateimagelabels:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    deps:
      - imgrefs-updateimagelabels
    cmds:
      - cmd: |-
          set -euo pipefail

          if ! command -v buildah >/dev/null 2>&1; then
            echo "Buildah is not available" >&2
            exit 1
          fi

          buildah_opts=()

          if command -v fuse-overlayfs >/dev/null 2>&1; then
            buildah_opts+=("--storage-opt" "overlay.mount_program=$(command -v fuse-overlayfs)")
          fi

          if [[ -f .task-meta-imgrefs ]]; then
            cat .task-meta-imgrefs | (grep -v "^localhost" || :) | while read tag; do
              buildah "${buildah_opts[@]}" push "${tag}"
              echo "Pushed ${tag}"
            done
          else
            echo "No .task-meta-imgrefs file - nothing will be pushed"
            exit 1
          fi
  imgrefs:
    cmds:
      - task: imgrefs-applybranchprotection
      - task: imgrefs-automergeprs
      - task: imgrefs-generatetasksandci
      - task: imgrefs-managegotools
      - task: imgrefs-setgotoolchain
      - task: imgrefs-updateimagelabels
  imgrefs-applybranchprotection:
    dir: '{{.ROOT_DIR}}/apply-branch-protection'
    cmds:
      - cmd: |-
          set -euo pipefail

          if [[ -f .task-meta-imgrefs ]] && [[ ${CI+y} == "y" ]]; then
            echo "Skipping re-computing tags"
            exit 0
          fi

          if ! command -v git >/dev/null 2>&1; then
            echo "Cannot find git" >&2
            exit 1
          fi

          if ! git describe --tags >/dev/null 2>&1; then
            echo "No git tags to descibe" >&2
            exit 1
          fi

          if ! grep ".task-meta-*" .gitignore >/dev/null 2>&1; then
            echo ".gitignore must include .task-meta-* to use the image builder tasks" >&2
            exit 1
          fi

          img_name=$( (grep "LABEL image.name=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)
          img_registry=$( (grep "LABEL image.registry=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)

          version=$(git describe --tags)
          is_exact_tag=$(git describe --tags --exact-match >/dev/null 2>&1 && echo y || echo n)
          major_version=$(echo "${version}" | cut -d '.' -f 1)
          latest_version_overall=$(git tag -l | sort -V | tail -n 1)
          latest_version_within_major=$(git tag -l | grep "^${major_version}" | sort -V | tail -n 1)

          echo -n "" > .task-meta-imgrefs

          if [[ ! -z "$img_name" ]]; then
            echo "localhost/${img_name}" >> .task-meta-imgrefs
            echo "localhost/${img_name}:${version}" >> .task-meta-imgrefs

            if [[ ! -z "$img_registry" ]] && [[ ${CI+y} == "y" ]]; then
              echo "${img_registry}/${img_name}:${version}" >> .task-meta-imgrefs

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_within_major}" ]]; then
                echo "${img_registry}/${img_name}:${major_version}" >> .task-meta-imgrefs
              fi

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_overall}" ]]; then
                echo "${img_registry}/${img_name}:latest" >> .task-meta-imgrefs
              fi
            fi
          else
            echo "Warning: no image name label; image will not be tagged" >&2
          fi

          echo "Image refs:"
          cat .task-meta-imgrefs | grep "." || echo "None"
  imgrefs-automergeprs:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    cmds:
      - cmd: |-
          set -euo pipefail

          if [[ -f .task-meta-imgrefs ]] && [[ ${CI+y} == "y" ]]; then
            echo "Skipping re-computing tags"
            exit 0
          fi

          if ! command -v git >/dev/null 2>&1; then
            echo "Cannot find git" >&2
            exit 1
          fi

          if ! git describe --tags >/dev/null 2>&1; then
            echo "No git tags to descibe" >&2
            exit 1
          fi

          if ! grep ".task-meta-*" .gitignore >/dev/null 2>&1; then
            echo ".gitignore must include .task-meta-* to use the image builder tasks" >&2
            exit 1
          fi

          img_name=$( (grep "LABEL image.name=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)
          img_registry=$( (grep "LABEL image.registry=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)

          version=$(git describe --tags)
          is_exact_tag=$(git describe --tags --exact-match >/dev/null 2>&1 && echo y || echo n)
          major_version=$(echo "${version}" | cut -d '.' -f 1)
          latest_version_overall=$(git tag -l | sort -V | tail -n 1)
          latest_version_within_major=$(git tag -l | grep "^${major_version}" | sort -V | tail -n 1)

          echo -n "" > .task-meta-imgrefs

          if [[ ! -z "$img_name" ]]; then
            echo "localhost/${img_name}" >> .task-meta-imgrefs
            echo "localhost/${img_name}:${version}" >> .task-meta-imgrefs

            if [[ ! -z "$img_registry" ]] && [[ ${CI+y} == "y" ]]; then
              echo "${img_registry}/${img_name}:${version}" >> .task-meta-imgrefs

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_within_major}" ]]; then
                echo "${img_registry}/${img_name}:${major_version}" >> .task-meta-imgrefs
              fi

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_overall}" ]]; then
                echo "${img_registry}/${img_name}:latest" >> .task-meta-imgrefs
              fi
            fi
          else
            echo "Warning: no image name label; image will not be tagged" >&2
          fi

          echo "Image refs:"
          cat .task-meta-imgrefs | grep "." || echo "None"
  imgrefs-generatetasksandci:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    cmds:
      - cmd: |-
          set -euo pipefail

          if [[ -f .task-meta-imgrefs ]] && [[ ${CI+y} == "y" ]]; then
            echo "Skipping re-computing tags"
            exit 0
          fi

          if ! command -v git >/dev/null 2>&1; then
            echo "Cannot find git" >&2
            exit 1
          fi

          if ! git describe --tags >/dev/null 2>&1; then
            echo "No git tags to descibe" >&2
            exit 1
          fi

          if ! grep ".task-meta-*" .gitignore >/dev/null 2>&1; then
            echo ".gitignore must include .task-meta-* to use the image builder tasks" >&2
            exit 1
          fi

          img_name=$( (grep "LABEL image.name=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)
          img_registry=$( (grep "LABEL image.registry=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)

          version=$(git describe --tags)
          is_exact_tag=$(git describe --tags --exact-match >/dev/null 2>&1 && echo y || echo n)
          major_version=$(echo "${version}" | cut -d '.' -f 1)
          latest_version_overall=$(git tag -l | sort -V | tail -n 1)
          latest_version_within_major=$(git tag -l | grep "^${major_version}" | sort -V | tail -n 1)

          echo -n "" > .task-meta-imgrefs

          if [[ ! -z "$img_name" ]]; then
            echo "localhost/${img_name}" >> .task-meta-imgrefs
            echo "localhost/${img_name}:${version}" >> .task-meta-imgrefs

            if [[ ! -z "$img_registry" ]] && [[ ${CI+y} == "y" ]]; then
              echo "${img_registry}/${img_name}:${version}" >> .task-meta-imgrefs

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_within_major}" ]]; then
                echo "${img_registry}/${img_name}:${major_version}" >> .task-meta-imgrefs
              fi

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_overall}" ]]; then
                echo "${img_registry}/${img_name}:latest" >> .task-meta-imgrefs
              fi
            fi
          else
            echo "Warning: no image name label; image will not be tagged" >&2
          fi

          echo "Image refs:"
          cat .task-meta-imgrefs | grep "." || echo "None"
  imgrefs-managegotools:
    dir: '{{.ROOT_DIR}}/manage-go-tools'
    cmds:
      - cmd: |-
          set -euo pipefail

          if [[ -f .task-meta-imgrefs ]] && [[ ${CI+y} == "y" ]]; then
            echo "Skipping re-computing tags"
            exit 0
          fi

          if ! command -v git >/dev/null 2>&1; then
            echo "Cannot find git" >&2
            exit 1
          fi

          if ! git describe --tags >/dev/null 2>&1; then
            echo "No git tags to descibe" >&2
            exit 1
          fi

          if ! grep ".task-meta-*" .gitignore >/dev/null 2>&1; then
            echo ".gitignore must include .task-meta-* to use the image builder tasks" >&2
            exit 1
          fi

          img_name=$( (grep "LABEL image.name=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)
          img_registry=$( (grep "LABEL image.registry=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)

          version=$(git describe --tags)
          is_exact_tag=$(git describe --tags --exact-match >/dev/null 2>&1 && echo y || echo n)
          major_version=$(echo "${version}" | cut -d '.' -f 1)
          latest_version_overall=$(git tag -l | sort -V | tail -n 1)
          latest_version_within_major=$(git tag -l | grep "^${major_version}" | sort -V | tail -n 1)

          echo -n "" > .task-meta-imgrefs

          if [[ ! -z "$img_name" ]]; then
            echo "localhost/${img_name}" >> .task-meta-imgrefs
            echo "localhost/${img_name}:${version}" >> .task-meta-imgrefs

            if [[ ! -z "$img_registry" ]] && [[ ${CI+y} == "y" ]]; then
              echo "${img_registry}/${img_name}:${version}" >> .task-meta-imgrefs

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_within_major}" ]]; then
                echo "${img_registry}/${img_name}:${major_version}" >> .task-meta-imgrefs
              fi

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_overall}" ]]; then
                echo "${img_registry}/${img_name}:latest" >> .task-meta-imgrefs
              fi
            fi
          else
            echo "Warning: no image name label; image will not be tagged" >&2
          fi

          echo "Image refs:"
          cat .task-meta-imgrefs | grep "." || echo "None"
  imgrefs-setgotoolchain:
    dir: '{{.ROOT_DIR}}/set-go-toolchain'
    cmds:
      - cmd: |-
          set -euo pipefail

          if [[ -f .task-meta-imgrefs ]] && [[ ${CI+y} == "y" ]]; then
            echo "Skipping re-computing tags"
            exit 0
          fi

          if ! command -v git >/dev/null 2>&1; then
            echo "Cannot find git" >&2
            exit 1
          fi

          if ! git describe --tags >/dev/null 2>&1; then
            echo "No git tags to descibe" >&2
            exit 1
          fi

          if ! grep ".task-meta-*" .gitignore >/dev/null 2>&1; then
            echo ".gitignore must include .task-meta-* to use the image builder tasks" >&2
            exit 1
          fi

          img_name=$( (grep "LABEL image.name=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)
          img_registry=$( (grep "LABEL image.registry=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)

          version=$(git describe --tags)
          is_exact_tag=$(git describe --tags --exact-match >/dev/null 2>&1 && echo y || echo n)
          major_version=$(echo "${version}" | cut -d '.' -f 1)
          latest_version_overall=$(git tag -l | sort -V | tail -n 1)
          latest_version_within_major=$(git tag -l | grep "^${major_version}" | sort -V | tail -n 1)

          echo -n "" > .task-meta-imgrefs

          if [[ ! -z "$img_name" ]]; then
            echo "localhost/${img_name}" >> .task-meta-imgrefs
            echo "localhost/${img_name}:${version}" >> .task-meta-imgrefs

            if [[ ! -z "$img_registry" ]] && [[ ${CI+y} == "y" ]]; then
              echo "${img_registry}/${img_name}:${version}" >> .task-meta-imgrefs

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_within_major}" ]]; then
                echo "${img_registry}/${img_name}:${major_version}" >> .task-meta-imgrefs
              fi

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_overall}" ]]; then
                echo "${img_registry}/${img_name}:latest" >> .task-meta-imgrefs
              fi
            fi
          else
            echo "Warning: no image name label; image will not be tagged" >&2
          fi

          echo "Image refs:"
          cat .task-meta-imgrefs | grep "." || echo "None"
  imgrefs-updateimagelabels:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    cmds:
      - cmd: |-
          set -euo pipefail

          if [[ -f .task-meta-imgrefs ]] && [[ ${CI+y} == "y" ]]; then
            echo "Skipping re-computing tags"
            exit 0
          fi

          if ! command -v git >/dev/null 2>&1; then
            echo "Cannot find git" >&2
            exit 1
          fi

          if ! git describe --tags >/dev/null 2>&1; then
            echo "No git tags to descibe" >&2
            exit 1
          fi

          if ! grep ".task-meta-*" .gitignore >/dev/null 2>&1; then
            echo ".gitignore must include .task-meta-* to use the image builder tasks" >&2
            exit 1
          fi

          img_name=$( (grep "LABEL image.name=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)
          img_registry=$( (grep "LABEL image.registry=" Containerfile || echo) | tail -n 1 | cut -d '=' -f 2-)

          version=$(git describe --tags)
          is_exact_tag=$(git describe --tags --exact-match >/dev/null 2>&1 && echo y || echo n)
          major_version=$(echo "${version}" | cut -d '.' -f 1)
          latest_version_overall=$(git tag -l | sort -V | tail -n 1)
          latest_version_within_major=$(git tag -l | grep "^${major_version}" | sort -V | tail -n 1)

          echo -n "" > .task-meta-imgrefs

          if [[ ! -z "$img_name" ]]; then
            echo "localhost/${img_name}" >> .task-meta-imgrefs
            echo "localhost/${img_name}:${version}" >> .task-meta-imgrefs

            if [[ ! -z "$img_registry" ]] && [[ ${CI+y} == "y" ]]; then
              echo "${img_registry}/${img_name}:${version}" >> .task-meta-imgrefs

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_within_major}" ]]; then
                echo "${img_registry}/${img_name}:${major_version}" >> .task-meta-imgrefs
              fi

              if [[ "${is_exact_tag}" == "y" ]] && [[ "${version}" == "${latest_version_overall}" ]]; then
                echo "${img_registry}/${img_name}:latest" >> .task-meta-imgrefs
              fi
            fi
          else
            echo "Warning: no image name label; image will not be tagged" >&2
          fi

          echo "Image refs:"
          cat .task-meta-imgrefs | grep "." || echo "None"
  lint:
    cmds:
      - task: lint-automergeprs-go
      - task: lint-generatetasksandci-go
      - task: lint-updateimagelabels-go
  lint-automergeprs:
    cmds:
      - task: lint-automergeprs-go
  lint-automergeprs-go:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    cmds:
      - cmd: |-
          exit_code=0

          # gofmt
          result=$(gofmt -e -s -d $(go list -f '{{ "{{.Dir}}" }}' ./... | grep -v /.go/ | grep -v /vendor/))
          if [[ ! -z "$result" ]]; then
            echo "## gofmt:"
            echo "$result"
            exit_code=1
          fi

          # staticcheck
          if grep staticcheck go.mod >/dev/null; then
            result=$(go tool staticcheck -checks inherit,+ST1003,+ST1016 ./... || true)
            if [[ ! -z "$result" ]]; then
              echo "## staticcheck:"
              echo "$result"
              exit_code=1
            fi
          fi

          # errcheck
          if grep errcheck go.mod >/dev/null; then
            result=$(go tool errcheck -ignoregenerated ./... || true)
            if [[ ! -z "$result" ]]; then
              echo "## errcheck:"
              echo "$result"
              exit_code=1
            fi
          fi

          exit $exit_code
  lint-generatetasksandci:
    cmds:
      - task: lint-generatetasksandci-go
  lint-generatetasksandci-go:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    cmds:
      - cmd: |-
          exit_code=0

          # gofmt
          result=$(gofmt -e -s -d $(go list -f '{{ "{{.Dir}}" }}' ./... | grep -v /.go/ | grep -v /vendor/))
          if [[ ! -z "$result" ]]; then
            echo "## gofmt:"
            echo "$result"
            exit_code=1
          fi

          # staticcheck
          if grep staticcheck go.mod >/dev/null; then
            result=$(go tool staticcheck -checks inherit,+ST1003,+ST1016 ./... || true)
            if [[ ! -z "$result" ]]; then
              echo "## staticcheck:"
              echo "$result"
              exit_code=1
            fi
          fi

          # errcheck
          if grep errcheck go.mod >/dev/null; then
            result=$(go tool errcheck -ignoregenerated ./... || true)
            if [[ ! -z "$result" ]]; then
              echo "## errcheck:"
              echo "$result"
              exit_code=1
            fi
          fi

          exit $exit_code
  lint-updateimagelabels:
    cmds:
      - task: lint-updateimagelabels-go
  lint-updateimagelabels-go:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    cmds:
      - cmd: |-
          exit_code=0

          # gofmt
          result=$(gofmt -e -s -d $(go list -f '{{ "{{.Dir}}" }}' ./... | grep -v /.go/ | grep -v /vendor/))
          if [[ ! -z "$result" ]]; then
            echo "## gofmt:"
            echo "$result"
            exit_code=1
          fi

          # staticcheck
          if grep staticcheck go.mod >/dev/null; then
            result=$(go tool staticcheck -checks inherit,+ST1003,+ST1016 ./... || true)
            if [[ ! -z "$result" ]]; then
              echo "## staticcheck:"
              echo "$result"
              exit_code=1
            fi
          fi

          # errcheck
          if grep errcheck go.mod >/dev/null; then
            result=$(go tool errcheck -ignoregenerated ./... || true)
            if [[ ! -z "$result" ]]; then
              echo "## errcheck:"
              echo "$result"
              exit_code=1
            fi
          fi

          exit $exit_code
  lintfix:
    cmds:
      - task: lintfix-automergeprs-go
      - task: lintfix-generatetasksandci-go
      - task: lintfix-updateimagelabels-go
  lintfix-automergeprs:
    cmds:
      - task: lintfix-automergeprs-go
  lintfix-automergeprs-go:
    dir: '{{.ROOT_DIR}}/auto-merge-prs'
    cmds:
      - cmd: gofmt -s -w .
  lintfix-generatetasksandci:
    cmds:
      - task: lintfix-generatetasksandci-go
  lintfix-generatetasksandci-go:
    dir: '{{.ROOT_DIR}}/generate-tasks-and-ci'
    cmds:
      - cmd: gofmt -s -w .
  lintfix-updateimagelabels:
    cmds:
      - task: lintfix-updateimagelabels-go
  lintfix-updateimagelabels-go:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    cmds:
      - cmd: gofmt -s -w .
  test:
    cmds:
      - task: test-updateimagelabels-go
  test-updateimagelabels:
    cmds:
      - task: test-updateimagelabels-go
  test-updateimagelabels-go:
    dir: '{{.ROOT_DIR}}/update-image-labels'
    cmds:
      - cmd: go test ./...
