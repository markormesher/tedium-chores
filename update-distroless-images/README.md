# Chore: Update Distroless Images

This [Tedium](https://github.com/markormesher/tedium) chore updates the [distroless](https://github.com/GoogleContainerTools/distroless) base image used for containers (e.g. to mass-migrate from `base` to `static`, or to update from `-debian12` to `-debian13`).
